# Copy this file to .env.local and fill in the values.
# The Vite dev server proxies /api/* to the Azure Functions backend,
# so you only need to change this if you're connecting to a deployed backend.
VITE_API_BASE_URL=
